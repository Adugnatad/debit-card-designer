import { Loader } from "lucide-react";

const LoaderComponent = () => {
  return (
    <Loader className="animate-spin m-auto" style={{ color: "#e48525" }} />
  );
};

export default LoaderComponent;
